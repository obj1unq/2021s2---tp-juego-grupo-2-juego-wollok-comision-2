import wollok.game.*
import personaje.*
import direcciones.*
import artefactos.*
import enemigos.*
import nivelesDelJuego.*

program juego {
	game.title("Demo")

	game.width(30)
    game.height(30)
    game.cellSize(30)
    game.boardGround("piso.jpg")
	demo.iniciar()
	game.start()
}