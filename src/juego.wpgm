import wollok.game.*
import personaje.*
import direcciones.*
import artefactos.*
import enemigos.*
import nivelesDelJuego.*

program juego {
	game.title("Demo")

	game.width(10)
    game.height(10)
    game.cellSize(64)
    game.boardGround("piso.jpg")
	demo.iniciar()
	game.start()
}